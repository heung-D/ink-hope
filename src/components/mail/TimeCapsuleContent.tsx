interface TimeCapsuleContentProps {
  onClose: () => void;
}

export function TimeCapsuleContent({ onClose }: TimeCapsuleContentProps) {
  return (
    <div className="flex-1 flex flex-col h-full overflow-hidden bg-muted/30">
      {/* 새로운 타임캡슐 UI를 여기에 추가하세요 */}
    </div>
  );
}
